import {ResultCodes} from "../m3-dal/api";

type TInitialState = {}

const initialState: TInitialState = {}

const authReducer = (state = initialState, action: any): TInitialState => {
    return state
}

export default authReducer