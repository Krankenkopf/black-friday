import {ResultCodes} from "../m3-dal/api";

type TInitialState = {}

const initialState: TInitialState = {}

export const passRecoveryReducer = (state = initialState, action: any): TInitialState => {
    return state
}
